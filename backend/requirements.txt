# WashApp Backend — FastAPI @ Render.com (Frankfurt)
# Sabit stack: Python/FastAPI · Supabase (Postgres+Auth+Storage+PostGIS+RLS) · Iyzico Pazaryeri (escrow)

# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0                 # prod: gunicorn -k uvicorn.workers.UvicornWorker

# --- Config & validation ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- DB: asyncpg (Supavisor transaction pooler, port 6543, statement_cache_size=0) ---
asyncpg==0.30.0

# --- Auth / JWT (Supabase JWKS ile JWT dogrulama) ---
pyjwt[crypto]==2.10.1            # RS256 JWKS dogrulama icin crypto extra'si sart
cryptography==44.0.0

# --- HTTP istemcisi (JWKS cek, Iyzico/PSP cagrilari) ---
httpx==0.28.1

# --- Supabase (Storage signed URL + Auth admin, service_role) ---
supabase==2.10.0

# --- Zamanlanmis gorevler (worker: 6 cron) ---
apscheduler==3.11.0

# --- Hata izleme ---
sentry-sdk[fastapi]==2.19.2

# --- Yardimcilar ---
python-dateutil==2.9.0.post0
orjson==3.10.12
