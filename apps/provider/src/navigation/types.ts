/** Hizmet veren app navigasyon param listesi (React Navigation 7 native-stack). */
export type ProviderStackParamList = {
  Home: undefined;
  // TODO(Faz-1): asil rotalar
  // JobPool: undefined;
  // Arrive: { orderId: string };
  // EvidenceCamera: { orderId: string; evre: "oncesi" | "sonrasi" };
  // Earnings: undefined;
};
