{
  "name": "@washapp/config",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo \"no tests yet\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
